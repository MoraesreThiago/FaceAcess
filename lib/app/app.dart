// Placeholder for the future root widget (`FaceAccessApp`).
//
// Intentionally empty in PR #1 — the goal here is only to materialize the
// new folder structure and wire `ProviderScope` at the root.
//
// The current `FaceAccessApp` keeps living in `lib/main.dart` until Fase 3
// of the refactor, when the composition root will be moved into this file.
